name: Disabled legacy Jekyll Pages workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This legacy Jekyll workflow is disabled. Use .github/workflows/static.yml for GitHub Pages deployment."
